from random import *

#sol = [1,2,3,5,4,0,6,8,7]

def verifica(sol):
    tam = len(sol)
    for i in range(tam):
        
    
    
    return

def SA(Demanda, Transportation, cliNum, facNum, Capacity):
    Demand = Demanda[:]
    Transp = Transportation[:]
    Cap = Capacity [:]
    flag = True
    sol = GRASP()
    aux = funcObj(sol)
    cli = randint(0, cliNum -1)
    fac = randint(0, facNum -1)
    a = verifica(sol)
    while sol[cli] ==  fac and a == 0) :
        cli = randint(0, cliNum -1)
        fac = randint(0, facNum -1)
        a(verifica())
        
            
            
    
    sol[cli] = fac
    aux2 = funcObj(sol)
